# Get random number betwen 1 and 100

import random

def bot_roll(mess, nick, botCmd):
    """Get a random number between 1 and 100"""
    random.seed()
    roll = random.randint(1, 100)
    if (len(botCmd) > 1):
        event = botCmd[1]
        message = nick + u' 对 ' + event + u' 进行判定，掷出 ' + str(roll) + u' 点。'    
    else:
        message = nick + u' 掷出 ' + str(roll) + u' 点。'    
    return message
    pass